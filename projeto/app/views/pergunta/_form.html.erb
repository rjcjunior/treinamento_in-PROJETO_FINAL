<%= form_for(perguntum) do |f| %>
  <% if perguntum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(perguntum.errors.count, "error") %> prohibited this perguntum from being saved:</h2>

      <ul>
      <% perguntum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %>
    <%= f.text_field :titulo %>
  </div>

  <div class="field">
    <%= f.label :conteudo %>
    <%= f.text_area :conteudo %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
