<%= form_for(votos_artigo) do |f| %>
  <% if votos_artigo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(votos_artigo.errors.count, "error") %> prohibited this votos_artigo from being saved:</h2>

      <ul>
      <% votos_artigo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :artigo_id %>
    <%= f.number_field :artigo_id %>
  </div>

  <div class="field">
    <%= f.label :votoArtigo %>
    <%= f.check_box :votoArtigo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
